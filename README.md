# mcpserver-claude-calendar-API

This is an MCP (Model Context Protocol) server for Google Calendar integration.

## Features
- List Google Calendar events
- Create new events
- Authenticate via Google OAuth

## Setup
1. Clone the repository
2. `npm install`
3. Configure `credentials.json` (not included)
4. Run `node src/index.js`
